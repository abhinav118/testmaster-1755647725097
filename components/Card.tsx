interface CardProps {
  title?: any
  subtitle?: any
  actions?: any
}

export default function Card({ title, subtitle, actions }: CardProps) {
  return (
    <div className="">
      {/* Card component content */}
    </div>
  )
}