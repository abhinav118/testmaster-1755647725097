interface ModalProps {
  open?: any
  title?: any
  size?: any
}

export default function Modal({ open, title, size }: ModalProps) {
  return (
    <div className="">
      {/* Modal component content */}
    </div>
  )
}